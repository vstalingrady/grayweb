import type { SidebarHistorySection } from "./types";

/**
 * Provides an empty history scaffold so the UI renders without seeded content.
 */
export const DEFAULT_HISTORY_SECTIONS: SidebarHistorySection[] = [];
