export type CalendarViewMode = "week" | "day";

export type WeekNowIndicator = { offset: number; dayIndex: number };

export type ComposerAnchorRect = {
  top: number;
  left: number;
  width: number;
  height: number;
};
