# AI Assistant Instructions

## Server Management

**DO NOT start or manage these processes:**
- `npm run dev` (frontend Next.js server)
- `npm run backend` or `npm run backend:with-mcp` (backend Python server)
- Do not create screen sessions for these servers
- Do not kill or restart these servers unless explicitly asked

The user manages their own development servers.

## Notes
- User preference added: 2025-11-25
